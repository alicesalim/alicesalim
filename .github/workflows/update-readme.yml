name: Update GitHub Profile Data

on:
  schedule:
    - cron: '0 */12 * * *'  # Atualiza a cada 12 horas
  workflow_dispatch:         # Permite rodar manualmente

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Force README refresh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          echo "<!-- updated $(date) -->" >> README.md
          git add README.md || echo "Nada para adicionar"
          git commit -m "🔄 Atualiza dados do perfil" || echo "Nada para commitar"
          git pull --rebase origin main || echo "Nada para puxar"
          git push origin main || echo "Push falhou, verifique permissões"  
